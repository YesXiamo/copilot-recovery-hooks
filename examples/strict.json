{
  "_comment": "严格模式 — 在 default 基础上,加大破坏性命令黑名单 + 收紧预算到 30 轮。适合生产/共享仓库。",
  "version": 1,
  "hooks": {
    "sessionStart":         [{ "command": "./hooks/session-resume.sh" }],
    "sessionEnd":           [{ "command": "./hooks/session-finalize.sh" }],
    "beforeSubmitPrompt":   [{ "command": "./hooks/budget-guard.sh", "failClosed": true, "_env_budget": 30 }],
    "preToolUse":           [{ "command": "./hooks/tool-guard.sh",
                               "matcher": "Shell|Read|Write|Edit|Grep", "timeout": 5 }],
    "postToolUse":          [{ "command": "./hooks/checkpoint.sh" }],
    "afterFileEdit":        [{ "command": "./hooks/checkpoint.sh" }],
    "postToolUseFailure":   [{ "command": "./hooks/failure-recorder.sh" }],
    "beforeShellExecution": [{ "command": "./hooks/shell-guard.sh",
                               "matcher": "rm -rf|DROP TABLE|TRUNCATE|kubectl delete|git push --force|git push -f|:>\\s|sudo |chmod 777|mv /|dd if=",
                               "failClosed": true }],
    "stop":                 [{ "command": "./hooks/auto-refine.sh", "loop_limit": 2 }]
  }
}
