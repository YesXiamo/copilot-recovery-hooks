{
  "_comment": "最小集 — 只保留 L3 自动反思 + L4 checkpoint。适合先尝鲜。",
  "version": 1,
  "hooks": {
    "postToolUse": [
      { "command": "./hooks/checkpoint.sh" }
    ],
    "postToolUseFailure": [
      { "command": "./hooks/failure-recorder.sh" }
    ],
    "stop": [
      { "command": "./hooks/auto-refine.sh", "loop_limit": 3 }
    ]
  }
}
